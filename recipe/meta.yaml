{% set version = "2.2.1" %}

package:
  name: pyparsing
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/p/pyparsing/pyparsing-{{ version }}.tar.gz
  sha256: f493ee323be1e94929416b3585eefcc04943115cecbaaa35a8c86d1a2368af19

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"

requirements:
  host:
    - python
    - pip
  run:
    - python

test:
  imports:
    - pyparsing

about:
  home: http://pyparsing.wikispaces.com/
  license: MIT
  # removed from the src tarball
  # See https://github.com/pyparsing/pyparsing/issues/31
  #license_file: LICENSE
  summary: Create and execute simple grammars
  description: |
    The pyparsing module is an alternative approach to creating and executing
    simple grammars, vs. the traditional lex/yacc approach, or the use of
    regular expressions. The pyparsing module provides a library of classes
    that client code uses to construct the grammar directly in Python code.
  doc_url: http://pyparsing.wikispaces.com/Documentation
  dev_url: https://svn.code.sf.net/p/pyparsing/code/

extra:
  recipe-maintainers:
    - msarahan
    - ocefpaf
